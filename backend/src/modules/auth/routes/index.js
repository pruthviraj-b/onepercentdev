// Authentication route registration is intentionally reserved for the next
// identity migration. Existing Google/Firebase and legacy API flows remain
// unchanged in this phase.
module.exports = function registerAuthRoutes() {};
