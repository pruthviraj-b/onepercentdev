# Validators

Request validation middleware is available in `middlewares/validation.js`. Endpoint-specific schemas should be added here as contracts are migrated without changing existing request acceptance rules.
