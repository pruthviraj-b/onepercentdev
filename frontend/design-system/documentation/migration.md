# Adoption plan

This phase only establishes the architecture. Existing pages remain unchanged. Future migrations should move one feature boundary at a time, replace local values with semantic tokens, adopt primitives, run visual regression checks, then remove the old styles only after parity is confirmed.
