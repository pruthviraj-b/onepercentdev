# Responsive contract

Breakpoints are mobile (0), tablet (640), laptop (1024), desktop (1280), and ultrawide (1536). Components should be fluid by default, use touch-safe targets on mobile, and avoid encoding page-specific layout assumptions.
