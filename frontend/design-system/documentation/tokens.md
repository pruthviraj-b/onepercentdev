# Tokens

Semantic tokens are grouped by color, typography, spacing, radius, shadow, gradient, animation, and breakpoint. The orange brand scale preserves the product identity while semantic surface, text, border, and status variables support light, dark, and high-contrast themes.

Use semantic variables in CSS and the exports from `design-system/tokens` in TypeScript. Avoid raw colors in new product components.
