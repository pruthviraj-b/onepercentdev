# Accessibility contract

New components must preserve keyboard operation, visible focus, semantic landmarks, accessible names, status/alert roles, and WCAG AA contrast. Motion is disabled through `prefers-reduced-motion`. High-contrast theme values are intentionally explicit and should be tested with keyboard navigation and screen readers.
