'use client';
import { Icon } from '@/design-system/icons/Icon';
import { useNavigation } from './NavigationProvider';
const notifications = [{ label: 'Learning', text: 'Your next lesson is ready to continue.', time: '2m' }, { label: 'Certificates', text: 'Your achievement certificate is available.', time: '1h' }, { label: 'AI', text: 'Your mentor has a new recommendation.', time: '3h' }];
export function NotificationCenter() { const { notificationsOpen, setNotificationsOpen } = useNavigation(); if (!notificationsOpen) return null; return <div className="shell-notifications" role="dialog" aria-label="Notifications"><div className="shell-notifications__header"><strong>Notifications</strong><button type="button" onClick={() => setNotificationsOpen(false)} aria-label="Close notifications"><Icon name="close" /></button></div><div className="shell-notifications__tabs"><button className="is-active" type="button">All</button><button type="button">Unread</button></div>{notifications.map((notification) => <div className="shell-notification" key={notification.text}><span className="shell-notification__icon"><Icon name={notification.label === 'AI' ? 'ai' : 'notifications'} /></span><div><strong>{notification.label}</strong><p>{notification.text}</p><small>{notification.time} ago</small></div></div>)}<button className="shell-notifications__all" type="button">View all notifications</button></div>; }
