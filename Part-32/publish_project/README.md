# OnePercent Utils

This is a simple practice package containing text utility functions from the OnePercent Python series.

## Usage

```python
from onepercentutils import clean_text, format_currency

print(clean_text("  Hello World  "))
print(format_currency(150000))
```
