[project]
name = "example-project"
version = "0.1.0"
description = "Module vs Package vs Library demo — managed with uv"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.0",
    "openai>=1.82.0",
    "python-dotenv>=1.1.0",
]
